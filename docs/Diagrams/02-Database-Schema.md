# 02 — Database Schema

Entity-relationship diagram of the PhotoGalleryDev database. Generated by inspecting `PhotoGallery/Data/ApplicationDbContext.cs`. Regenerate alongside any migration that adds or changes a table.

## Diagram

```mermaid
erDiagram
  AspNetUsers ||--o{ Albums : "owns"
  AspNetUsers ||--o{ UserCartItems : "has cart of"
  AspNetUsers ||--o{ SavedAccessCodes : "saves"
  AspNetUsers ||--o{ UserAccessLog : "audited as"

  Albums ||--o{ Photos : "contains"
  Albums ||--o{ AccessCodes : "shared via"

  Photos ||--o{ PhotoVersions : "has variants"
  Photos ||--|| PhotoFiles : "stores original"
  Photos ||--o{ PhotoVersionUrls : "caches URLs"
  Photos ||--o{ ProcessingQueues : "scheduled by"
  Photos ||--o{ Downloads : "downloaded as"
  Photos ||--o{ UserCartItems : "in carts"

  ProcessingQueues ||--o{ ProcessingQueueItems : "owns 5"
  AccessCodes ||--o{ SavedAccessCodes : "saved as"
  AccessCodes ||--o{ Downloads : "anchors"
  AccessCodes ||--o{ UserAccessLog : "audited via"

  AspNetUsers {
    string  Id PK
    string  Email
    string  UserName
    string  FirstName
    string  LastName
    int     LoginCount
    datetime LastLoginAt
  }

  Albums {
    Guid    Id PK
    string  Title
    string  Description
    Guid    OwnerId FK
    string  CreatedBy
    datetime CreatedDate
  }

  Photos {
    Guid    Id PK
    Guid    AlbumId FK
    string  FileName
    string  StorageKey
    datetime UploadDate
    string  UploadedBy
    string  Metadata
    int     ProcessingStatus  "Uploading/Pending/Processing/Complete/Failed"
    bool    HasThumbnail
    bool    HasLow
    bool    HasMedium
    bool    HasHigh
    datetime ProcessingStartedAt
    datetime ProcessingCompletedAt
    bool    ProcessingComplete
    byte[]  RowVersion
  }

  PhotoVersions {
    Guid    Id PK
    Guid    PhotoId FK
    int     Quality "Raw/Thumbnail/Low/Medium/High/Original/Watermark"
    string  StorageKey
    long    FileSize
    datetime ProcessedDate
  }

  PhotoFiles {
    Guid    Id PK
    Guid    PhotoId FK
    string  StorageKey
    long    FileSize
  }

  PhotoVersionUrls {
    Guid    Id PK
    Guid    PhotoId FK
    int     Quality
    string  PresignedUrl
    datetime ExpiresAt
    datetime GeneratedAt
    bool    IsActive
  }

  ProcessingQueues {
    Guid    Id PK
    Guid    PhotoId FK
    int     Status "Pending/Processing/Complete/Error"
    datetime CreatedAt
    datetime CompletedAt
    string  ErrorMessage
  }

  ProcessingQueueItems {
    Guid    Id PK
    Guid    PhotoId FK
    Guid    ProcessingQueueId FK
    int     Quality "Thumbnail/Low/Medium/High/Watermark"
    int     Status "Pending/Processing/Complete/Error"
    int     RetryCount
    string  LastError
    int     Attempts
    datetime NextRetryTime
    datetime LeaseExpiresAt
    datetime CompletedAt
    datetime CreatedAt
    datetime UpdatedAt
  }

  AccessCodes {
    Guid    Id PK
    Guid    AlbumId FK
    string  Code
    bool    IsActive
    bool    IsDeleted
    string  IssuedAdminEmail
    datetime IssuedAt
    datetime ExpiresAt
  }

  SavedAccessCodes {
    Guid    Id PK
    string  UserId FK
    Guid    AccessCodeId FK
    datetime SavedAt
    datetime LastUsedAt
  }

  Downloads {
    Guid    Id PK
    Guid    PhotoId FK
    Guid    AccessCodeId FK "nullable"
    string  UserId "nullable"
    int     Quality
    datetime DownloadedAt
    string  IpAddress
    string  UserAgent
  }

  UserAccessLog {
    Guid    Id PK
    string  UserId "nullable, null = anonymous visitor"
    Guid    AccessCodeId FK
    string  IpAddress
    string  UserAgent
    datetime AccessedAt
  }

  UserCartItems {
    Guid    Id PK
    string  UserId FK
    Guid    PhotoId FK
    Guid    SourceAlbumId FK
    int     Quality
    datetime AddedAt
  }

  AuditLogEntries {
    Guid    Id PK
    string  Actor
    string  Action
    string  TargetType
    string  TargetId
    string  DetailsJson
    datetime At
  }

  RuntimeSettings {
    string  Key PK
    string  Value
    datetime UpdatedAt
    string  UpdatedBy
  }

  WorkerHeartbeats {
    Guid    Id PK
    string  WorkerName "unique with InstanceId"
    string  InstanceId
    string  DisplayName
    int     IntervalSeconds
    datetime LastHeartbeatAt
    datetime LastRanAt
    long    ItemsProcessedTotal
    int     ItemsInFlight
    string  LastError
    double  CpuPercent
    long    WorkingSetBytes
    long    ManagedHeapBytes
  }

  AdminJobs {
    Guid    Id PK
    string  JobType "reconcile-storage / reconcile-album-storage / reap-orphans / chaos-storage / purge-failed-photos"
    Guid    AlbumId "nullable, required for reconcile-album-storage"
    string  Status "pending / running / complete / error"
    datetime RequestedAt
    string  RequestedBy "user email, or system:scheduler"
    datetime StartedAt
    datetime CompletedAt
    string  CompletedByInstanceId
    string  ResultJson
    string  ErrorMessage
  }
```

## Notes on the relationships

* **`PhotoVersions` vs `PhotoFiles`**: both are write-only today. They were intended to track derived variants and originals separately. Neither is read by any current code path. They are still on the schema so the cascade-delete chain in `FailedPhotoPurgeService` has somewhere to point. See `MEMORY.md` for the rationale.
* **`ProcessingQueueItems.LeaseExpiresAt`**: the lease mechanism. A NULL or expired value plus `Status = Processing` is an orphan. The reconciler resets it to `Pending`.
* **`AdminJobs` unique key**: there is no unique constraint on `(JobType, AlbumId)`. Deduplication is enforced at the controller layer in `EnqueueAdminJobAsync`. If a future requirement needs hard uniqueness, add an index.
* **`WorkerHeartbeats` unique key**: `(WorkerName, InstanceId)`. Enforced by `WorkerHeartbeatConfiguration`.
* **`AccessCodes.IsDeleted`**: soft delete. Cascade-deleting an album hard-deletes its codes. The soft flag exists for admin "undo" within an album.
* **`UserAccessLog.UserId` nullable**: null means anonymous visitor used an access code. Authenticated visits set the user id.

## Generation

This diagram is hand-maintained today. The `er-diagram-from-efcore` skill describes the path to auto-regenerating it from `dotnet ef dbcontext optimize` output, owned by the dba-efcore agent. Treat the auto-regenerated version as canonical when that lands.

## When to update

* Any migration that adds, removes, or renames a table.
* Any migration that adds, removes, or changes a foreign key.
* Any change to a column type that the diagram surfaces (string → enum, int → long, nullability flip).
* Pure additive non-FK columns can be left out of the diagram if they don't affect cardinality, but prefer adding them.
