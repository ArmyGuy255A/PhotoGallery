export const environment = {
  production: true,
  apiUrl: '',  // Use relative path in production (reverse proxy)
  googleClientId: ''  // populate via deployment-time env var or replace before deploy
};
