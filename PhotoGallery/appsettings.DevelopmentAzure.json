{
  "$schema": "https://json.schemastore.org/appsettings.json",

  "//": [
    "Azure-backed local-dev overlay. Activated automatically when",
    "ASPNETCORE_ENVIRONMENT=DevelopmentAzure (use the 'AzureDev' launch profile",
    "or set the env var before `dotnet run`).",
    "",
    "Placeholder values below are filled by the platform engineer in",
    "Documentation/Runbooks/local-azure-dev.md once Azure dev resources land.",
    "Do NOT commit real account names or URIs here — values come from Key Vault",
    "(secret name 'ConnectionStrings--DefaultConnection' for the DB) or from",
    "developer environment variables.",
    "",
    "DISABLE_AUTH is intentionally NOT set — real Google OAuth + JWT stay on so",
    "this profile exercises the production auth path."
  ],

  "KeyVault": {
    "Uri": "https://<TO-BE-FILLED>.vault.azure.net/"
  },

  "Storage": {
    "Provider": "AzureBlob",
    "AzureBlob": {
      "AccountUrl": "https://<TO-BE-FILLED>.blob.core.windows.net/",
      "ContainerName": "photogallery"
    }
  },

  "Database": {
    "Provider": "SqlServer"
  },

  "ConnectionStrings": {
    "DefaultConnection": "<resolved-from-KeyVault-at-runtime>"
  },

  "Email": {
    "Provider": "mock"
  }
}
